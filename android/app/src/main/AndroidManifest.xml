<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Supabase (auth + senkron) için gerekli. Debug manifestindeki izin yalnızca
         debug/profile derlemelerine uygulanır; release APK'da burada olmalı. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Cihaz yeniden baslatildiginda zamanlanmis bildirimin yeniden
         kurulmasi icin. POST_NOTIFICATIONS burada yok cunku
         flutter_local_notifications kendi manifestinde tanimliyor ve
         manifest birlestirici otomatik ekliyor.
         SCHEDULE_EXACT_ALARM / USE_EXACT_ALARM bilerek eklenmedi: gunluk
         hatirlatma icin inexact zamanlama yeterli, o izinler Play
         incelemesinde alarm/takvim uygulamalarina ayrilmis durumda. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- image_cropper: uCrop kırpma ekranı ayrı bir activity olarak açılıyor,
             manifest'te tanımlı olmazsa çağrıldığı anda çöker. -->
        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <!-- flutter_local_notifications: zamanlanmis bildirimi tetikleyen
             alarm alicisi. Manifest'te tanimli olmazsa bildirim hic calmaz. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false" />

        <!-- Reboot / uygulama guncellemesi sonrasi zamanlamayi yeniden kurar.
             Uygulama acilisindaki yeniden kurulum (studyReminderBootstrapProvider)
             ancak kullanici uygulamayi actiginda devreye girer; bu alici
             aradaki bosluğu kapatiyor. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
